# The client talks to the Python gateway's /api. In dev, Vite proxies /api to
# this target (see vite.config.ts). In Docker, nginx proxies /api to the gateway
# and this is unused.
VITE_API_TARGET=http://localhost:18080
